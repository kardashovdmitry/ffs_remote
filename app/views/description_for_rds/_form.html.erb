<%= form_for @descriptionForRd do |f| %>

  <% if @descriptionForRd.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@descriptionForRd.errors.count, "error") %> prohibited
        this device from being saved:
      </h2>
      <ul>
        <% @descriptionForRd.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :value %><br>
    <%= f.text_area :value %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
